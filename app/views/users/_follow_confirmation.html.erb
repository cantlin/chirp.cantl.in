<div class="system-message success">
  <%= form_tag user_path(@user), :id => 'undo' %>
  <%= hidden_field_tag '_method', 'put' -%>  
  <% params[:follow].each do |user| -%>
      <input type="hidden" name="unfollow[]" value="<%= user %>" />
  <% end -%>
  <p><%= t("you_followed") %> <%= raw comma_seperated_usernames params[:follow] %>. <%= raw t(:undo_or_dismiss_this, :undo_link => link_to(t(:undo_link_text), '', 'data-submit' => 'undo'), :dismiss_link => link_to(t(:dismiss_link_text), '', :'data-destroy' => "system-message.success")) %></p>
  </form>
</div>
